=== LINE 931 | TYPE CODE_ACTION ===
CONTENT:
Created At: 2026-07-18T15:45:56Z
Completed At: 2026-07-18T15:45:58Z
The following changes were made by the replace_file_content tool to: s:\RESUME_BUILDER_PROJECT\Resume-Tailor\gemini_service.py. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -331,43 +331,36 @@
     return line
 
 
-def find_matching_project_line(ai_name: str, master_resume: str) -> str | None:
+def find_full_project_title_from_blob(ai_name: str, master_resume: str) -> str | None:
     ai_name_lower = ai_name.lower().strip()
     core_name = re.split(r'[\u2013-]', ai_name_lower)[0].strip()
-    if not core_name:
-        return None
-        
-    master_lines = master_resume.splitlines()
-    
-    # 1. Try exact substring match with core name
-    for line in master_lines:
-        line_clean = line.strip()
-        if not line_clean:
-            continue
-        if core_name in line_clean.lower():
-            return line_clean
-            
-    # 2. Try match with first two words of the name if it is long
-    words = [w for w in re.split(r'[^\w\d]+', core_name) if w]
-    if len(words) >= 2:
-        two_words = " ".join(words[:2])
-        for line in master_lines:
-            line_clean = line.strip()
-            if not line_clean:
-                continue
-            if two_words in line_clean.lower():
-                return line_clean
-                
-    # 3. Try match with first word if it is unique/long enough
-    if words and len(words[0]) >= 4:
-        for line in master_lines:
-            line_clean = line.strip()
-            if not line_clean:
-                continue
-            if re.search(r'\b' + re.escape(words[0]) + r'\b', line_clean.lower()):
-                return line_clean
-                
-    return None
+    if not core_name or len(core_name) < 4:
+        return None
+        
+    master_lower = master_resume.lower()
+    start_idx = master_lower.find(core_name)
+    if start_idx == -1:
+        words = [w for w in re.split(r'[^\w\d]+', core_name) if w]
+        if words and len(words[0]) >= 4:
+            start_idx = master_lower.find(words[0])
+            
+    if start_idx == -1:
+        return None
+        
+    # Search forward up to 250 characters for a date range
+    search_sub = master_resume[start_idx:start_idx + 250]
+    date_pattern = r'\b(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)\s*\d{4}\s*(?:[-–—]|to)\s*(?:Present|(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)\s*\d{4})\b'
+    
+    match = re.search(date_pattern, search_sub, flags=re.IGNORECASE)
+    if match:
+        title_part = search_sub[:match.start()]
+    else:
+        title_part = search_sub.splitlines()[0]
+        
+    title_part = re.sub(r'^\s*[\d\.\-\*•]+\s*', '', title_part).strip()
+    title_part = re.sub(r'\s+', ' ', title_part).strip()
+    title_part = re.sub(r'\s*[-–—,]+\s*$', '', title_part).strip()
+    return title_part
 
 
 def restore_full_project_names(tailored_data: dict[str, Any], master_resume: str):
@@ -382,12 +382,10 @@
         if not ai_name:
             continue
             
-        best_match_line = find_matching_project_line(ai_name, master_resume)
-        if best_match_line:
-            full_title = clean_master_project_line(best_match_line)
-            if full_title and ai_name != full_title:
-                logger.info("Restoring project title: '%s' -> '%s'", ai_name, full_title)
-                proj["name"] = full_title
+        full_title = find_full_project_title_from_blob(ai_name, master_resume)
+        if full_title and ai_name != full_title:
+            logger.info("Restoring project title: '%s' -> '%s'", ai_name, full_title)
+            proj["name"] = full_title
 
 
 def validate_tailored_resume(tailored_data: dict[str, Any], master_resume: str, selected_keywords: list[str] | None = None) -> list[str]:
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.